#!/usr/bin/env bash
set -euo pipefail

export XDG_RUNTIME_DIR="/run/user/$(id -u)"
export MESA_LOADER_DRIVER_OVERRIDE=virtio
exec flatpak run com.valvesoftware.Steam -gamepadui -gamescope -w 1920 -h 1080 -r 60
